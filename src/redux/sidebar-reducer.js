let initialState = {
    friends: [
        {id: 1, name: 'Sasha'},
        {id: 2, name: 'Andrew'},
        {id: 3, name: 'Wilson'}
    ]
}
const sidebarReducer = (state = initialState, action) => {


    return state
}

export default sidebarReducer